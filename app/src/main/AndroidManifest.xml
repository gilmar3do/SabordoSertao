<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gilmarf.sabordosertao">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NovaVendaActivity">
        </activity>

        <activity android:name=".NovaDespoupaActivity">
        </activity>

        <activity android:name=".NovaDespesaActivity">
        </activity>

        <activity android:name=".NovoEnvaseActivity">
        </activity>

        <activity android:name=".NovoFuncionarioActivity">
        </activity>

        <activity android:name=".NovaFrutaActivity">
        </activity>

        <activity android:name=".NovoEstoqueActivity">
        </activity>

        <activity android:name=".NovoPrecoActivity">
        </activity>
    </application>

</manifest>